import LoginScreenTemplate from '../components/LoginScreenTemplate';

export default function RegisterScreen({ navigation }: any) {
  return (
    <LoginScreenTemplate navigation={navigation} screen="Register_Screen" />
  );
}
