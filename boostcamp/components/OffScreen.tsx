import { View, Text } from 'react-native';

export default function OffScreen({ navigation }: any) {
  // const onPressHandler = () => {
  //   navigation.goBack();
  // };

  return (
    <View>
      <Text>Offscreen Tasks Screen</Text>
    </View>
  );
}
